[project]
name = "physical-infra-ansible"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "ansible>=14.0.0",
    "ansible-lint>=26.4.0",
    "boto3>=1.43.0",
]

[tool.uv]
required-version = ">=0.10.0,<0.12.0"
