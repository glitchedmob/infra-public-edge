---
ansible_host: "{{ public_ssh_host if lookup('env', 'EDGE_CONNECTION_MODE') == 'public' else tailscale_ssh_host }}"
ansible_ssh_private_key: "{{ lookup('amazon.aws.aws_ssm', ssm_private_key_path) if ssm_private_key_path is defined else omit }}"
