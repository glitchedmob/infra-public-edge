[defaults]
inventory = ./inventory.yml
host_key_checking = False
interpreter_python = auto_silent
deprecation_warnings = False
collections_path = ./collections
roles_path = ./roles

[connection]
ssh_agent = auto

[ssh_connection]
pipelining = True
ssh_common_args = -o IdentityAgent=$SSH_AUTH_SOCK
