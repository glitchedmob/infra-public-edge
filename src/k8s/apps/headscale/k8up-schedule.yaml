apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: headscale-backup
  namespace: headscale
spec:
  backend:
    repoPasswordSecretRef:
      name: headscale-k8up-repo-password
      key: password
    b2:
      bucket: levizitting-public-edge-backups
      path: headscale
      accountIDSecretRef:
        name: k8up-b2-credentials
        key: account-id
      accountKeySecretRef:
        name: k8up-b2-credentials
        key: account-key
  backup:
    schedule: '0 8 * * *'
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 2
    labelSelectors:
      - matchLabels:
          backup.k8up.io/app: headscale
  check:
    schedule: '0 9 * * 1'
  prune:
    schedule: '0 10 * * 0'
    retention:
      keepLast: 5
      keepDaily: 14
      keepWeekly: 4
