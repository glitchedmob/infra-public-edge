apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: headscale-shared-tls
  namespace: headscale
spec:
  secretName: headscale-shared-tls
  dnsNames:
    - headscale.levizitting.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
