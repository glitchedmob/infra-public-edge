name: Ansible Lint

on:
  push:
    branches:
      - main
    paths:
      - src/ansible/**
      - .github/workflows/ansible-lint.yml

permissions:
  contents: read
  id-token: write

jobs:
  lint:
    uses: glitchedmob/infra-gha/.github/workflows/ansible-lint.yml@v0.7.4
    with:
      working-directory: src/ansible
